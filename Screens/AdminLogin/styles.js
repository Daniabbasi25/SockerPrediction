import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    body: {
        margin: 20,
        // borderWidth: 1,
        flex: 1,
    },
    cardHead: {
        fontSize: 20,
        fontWeight: "700",
        marginBottom: 10,
    },
    form: {
        marginTop: 20,
    },
    input: {
        marginVertical: 20,
    }
});

export default styles;